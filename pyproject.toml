[project]
name = "komorebi-flow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flowlauncher>=0.2.0",
    "pip>=25.3",
]
